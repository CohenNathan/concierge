# Troubleshooting Guide - Solomon не се отваря (Solomon won't open)

## Problem: Празна страница (Blank Page)

Ако виждате празна страница, следвайте тези стъпки:

### Опция 1: Тествайте без сървър (TEST_FEATURES.html)

**Най-бързото решение - отворете директно в браузър:**

```bash
# В браузъра отворете:
file:///home/runner/work/concierge/concierge/TEST_FEATURES.html

# Или от терминала:
xdg-open TEST_FEATURES.html
# или
firefox TEST_FEATURES.html
# или
chrome TEST_FEATURES.html
```

Този файл показва всички функции без да се нуждае от сървър или API ключове.

---

### Опция 2: Проверете дали сървърът работи

#### A. Проверете дали сървърът е стартиран:

```bash
# Проверете за работещи процеси
ps aux | grep -E "python.*main|uvicorn|demo.py"

# Проверете дали портът е зает
netstat -tuln | grep -E "8000|8001"
# или
lsof -i :8000
lsof -i :8001
```

#### B. Тествайте връзката:

```bash
# Тест на demo сървъра (port 8001)
curl http://localhost:8001/

# Тест на main сървъра (port 8000)
curl http://localhost:8000/
```

---

### Опция 3: Стартирайте demo сървъра правилно

#### Метод 1: Директно с Python

```bash
cd /home/runner/work/concierge/concierge
python3 demo.py
```

Трябва да видите:
```
╔══════════════════════════════════════════════════════════════╗
║         Solomon DEMO MODE - Cohen House Concierge           ║
╚══════════════════════════════════════════════════════════════╝

🐻 Starting demo server...
📍 Open: http://localhost:8001
```

След това отворете: http://localhost:8001

#### Метод 2: Ако има грешка с порт 8001

```bash
# Използвайте друг порт
python3 -c "
from demo import app
import uvicorn
uvicorn.run(app, host='0.0.0.0', port=8002)
"
```

След това отворете: http://localhost:8002

---

### Опция 4: Стартирайте пълния сървър (с OpenAI API)

#### Стъпка 1: Проверете зависимости

```bash
pip3 list | grep -E "fastapi|uvicorn|openai"
```

Ако липсват:
```bash
pip3 install fastapi uvicorn python-dotenv openai aiohttp pydantic
```

#### Стъпка 2: Конфигурирайте .env

```bash
cp .env.example .env
nano .env  # или vim .env, или gedit .env
```

Добавете:
```
OPENAI_API_KEY=sk-вашият-ключ-тук
```

#### Стъпка 3: Стартирайте

```bash
./start.sh
```

Или директно:
```bash
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

### Опция 5: Дебъг грешки

#### Проверете грешки в конзолата:

```bash
# Стартирайте с verbose изход
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --log-level debug
```

#### Чести проблеми:

**Проблем: "ModuleNotFoundError: No module named 'openai'"**
```bash
# Решение:
pip3 install openai
```

**Проблем: "The api_key client option must be set"**
```bash
# Решение: Създайте .env файл
echo "OPENAI_API_KEY=sk-your-key-here" > .env
```

**Проблем: "Address already in use" (портът е зает)**
```bash
# Решение: Спрете другия процес или използвайте друг порт
lsof -ti:8000 | xargs kill -9
# или използвайте друг порт
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8888
```

**Проблем: Празна страница в браузъра**
```bash
# Проверете:
# 1. Дали сървърът действително работи
curl http://localhost:8000/

# 2. Проверете конзолата на браузъра (F12) за JavaScript грешки

# 3. Проверете дали файловете съществуват
ls -la web/solomon.html
ls -la web/avatar.glb
```

---

### Опция 6: Използвайте тестовата страница с всички функции

```bash
# Отворете TEST_FEATURES.html директно в браузър
firefox TEST_FEATURES.html

# Или копирайте пътя и го поставете в браузъра:
# file:///home/runner/work/concierge/concierge/TEST_FEATURES.html
```

Този файл:
- ✅ Не се нуждае от сървър
- ✅ Не се нуждае от API ключове
- ✅ Показва всички имплементирани функции
- ✅ Интерактивен - можете да кликате бутоните
- ✅ Работи веднага

---

## Кратко обобщение (Quick Summary)

### Най-бързо решение:
```bash
# Просто отворете този файл в браузър:
firefox TEST_FEATURES.html
```

### За demo сървър:
```bash
python3 demo.py
# Отворете: http://localhost:8001
```

### За пълен сървър:
```bash
# 1. Добавете API ключ в .env
echo "OPENAI_API_KEY=sk-..." > .env

# 2. Стартирайте
./start.sh
# Отворете: http://localhost:8000
```

---

## Проверка че всичко работи

След стартиране на сървъра, трябва да видите в конзолата:

```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
✅ Solomon ready
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

И в браузъра на http://localhost:8000 или http://localhost:8001 трябва да видите интерфейса на Solomon.

---

🐻 **За помощ:** Всички функции са тествани и работят. Вижте `VERIFICATION_CHECKLIST.md` за детайли.
