# Cohen House Concierge - Кратко ръководство (Bulgarian Quick Guide)

## 🚀 Бързо качване на отдалечен компютър чрез SSH

### Стъпка 1: Подготовка

```bash
# Клониране на проекта
git clone https://github.com/CohenNathan/concierge.git
cd concierge
```

### Стъпка 2: Настройка на качването

```bash
# Стартиране на скрипта за първи път (създава конфигурация)
./deploy.sh

# Редактиране на конфигурацията
nano deploy.config
```

**Редактирайте тези настройки:**
```bash
REMOTE_USER="вашето_потребителско_име"
REMOTE_HOST="IP_адрес_на_сървъра"
REMOTE_PORT="22"
REMOTE_PATH="/var/www/concierge"
```

### Стъпка 3: Качване

```bash
# Качване на проекта
./deploy.sh
```

### Стъпка 4: Настройка на сървъра

```bash
# Влизане в сървъра
ssh your_user@your_server

# Създаване на .env файл
cd /var/www/concierge
cp .env.example .env
nano .env

# Добавете вашите API ключове:
# OPENAI_API_KEY=sk-proj-...
# ELEVENLABS_API_KEY=...

# Инсталиране на зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Стартиране
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

## 📚 Пълна документация

- **SSH_DEPLOYMENT_GUIDE.md** - Пълно ръководство на български и английски
- **DEPLOYMENT.md** - Production настройка
- **README.md** - Пълна документация на английски

## 🆘 Помощ

### Проблеми с връзката
```bash
# Тест на SSH връзката
ssh your_user@your_server

# Генериране на SSH ключ
ssh-keygen -t rsa -b 4096
ssh-copy-id your_user@your_server
```

### Проблеми с качването
```bash
# Проверка дали rsync е инсталиран
rsync --version

# Инсталация на rsync
# Ubuntu/Debian:
sudo apt-get install rsync

# macOS:
brew install rsync
```

### Проблеми със сървъра
```bash
# Преглед на логове
ssh your_user@your_server "sudo journalctl -u concierge -n 50"

# Рестартиране
ssh your_user@your_server "sudo systemctl restart concierge"

# Проверка на статуса
ssh your_user@your_server "sudo systemctl status concierge"
```

## ✅ Checklist

- [ ] Проектът клониран локално
- [ ] deploy.config настроен
- [ ] SSH връзката работи
- [ ] Файловете качени с ./deploy.sh
- [ ] .env файл създаден на сървъра с API ключове
- [ ] Зависимостите инсталирани
- [ ] Сървърът стартира
- [ ] Интерфейсът се зарежда на http://server_ip:8000/solomon.html

## 🎉 Готово!

Вашият Cohen House Concierge е качен и работи!

**Достъп:** http://your_server_ip:8000/solomon.html

---

*За подробна информация вижте SSH_DEPLOYMENT_GUIDE.md*
